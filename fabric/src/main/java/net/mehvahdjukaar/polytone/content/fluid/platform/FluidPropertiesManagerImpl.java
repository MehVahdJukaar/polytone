package net.mehvahdjukaar.polytone.content.fluid.platform;

import net.mehvahdjukaar.polytone.content.fluid.FluidPropertyModifier;
import net.minecraft.world.level.material.Fluid;

// Stub - fabric fluid render handler API was completely rewritten in 26.1
// (FluidRenderHandlerRegistry removed; new FluidRendering.render() pipeline)
// TODO: reimplement against new FluidRendering API
public class FluidPropertiesManagerImpl {

    public static void tryAddSpecial(Fluid fluid, FluidPropertyModifier fluidMod) {
    }

    public static void clearSpecial() {
    }
}
